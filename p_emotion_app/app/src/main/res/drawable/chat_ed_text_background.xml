<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF"/> <!-- 白色背景 -->
    <corners android:radius="8dp"/> <!-- 圆角 -->
    <!-- 边框宽度 -->
    <stroke
        android:width="1dp"
    android:color="#000000" /> <!-- 边框颜色设置为黑色 -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>
</shape>